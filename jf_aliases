function jf-docker() {
  local _target=${1}
  docker run -i \
    -v /:/var/lib/jf \
    -e JF_PATH_BASE="/var/lib/jf" \
    -e JF_PATH="${JF_PATH}" \
    -e JF_DEBUG=${JF_DEBUG} \
    -e JF_CWD="$(pwd)" \
    dakusui/jf:"${JF_DOCKER_TAG:-v0.1}" "${@}"
}
